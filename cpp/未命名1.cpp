#include<iostream>
using namespace std;
//函数重载
//重载就是：一个函数可以有多种写法，一般是函数名相同，参数不同。
int add0(int x, int y);
float add0(float x, float y);
 //形参类型不同
int add0(int x, int y);
int add0(int x, int y, int z);
//形参个数不同

//带默认参数值的函数
/*int add(int x = 5, int y = 6) {
    return x + y;
}
int main() {
    add(10,20);  // 10 + 20 = 30
    add(10);     // 10 + 6 = 16
    add();       // 5 + 6 = 11
    return 0;
}*/

int main(){
	//new delete用于堆空间的分配与回收
    //new：用于从堆中分配指定大小的内存区域，并返回获得内存区域的首地址。
	//相较于 malloc 可以自动计算大小、无需指针转换。
	//示例：int *arr = new int[100];
	//delete：用于释放 new 分配的堆内存。
	//示例：delete[] arr;
	
	
	
	
	
	
	
	
} 
